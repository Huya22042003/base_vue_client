export type CodeMngModel = {
    cdId : string | number,
    cdNm : string | number,
    upCdId: string | number,
}

export type CodeMngRes = {
    upCdId: string | number
}

export type CodeMngResModel = {
    upCdIdList: string[];
}